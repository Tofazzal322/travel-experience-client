import React, { useState } from 'react';

const useData = () => {
    const [data, setData] = useState([]);
    console.log(data);

    use
    

    return (
        <div>
            
        </div>
    );
};

export default useData;